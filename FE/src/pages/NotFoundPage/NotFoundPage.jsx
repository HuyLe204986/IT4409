import React from 'react'
const NotFoundPage = () => {
  const style = {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: '100vh',
    fontSize: '24px',
    fontWeight: 'bold',
    color: '#333',
  };

  return (
    <div style={style}>
      Not Found Page
    </div>
  );
};

export default NotFoundPage